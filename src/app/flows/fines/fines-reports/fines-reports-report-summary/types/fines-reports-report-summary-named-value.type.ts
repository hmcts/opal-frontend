/**
 * Holds an intermediate report-summary value before it is converted into a template row.
 */
export type FinesReportsReportSummaryNamedValue = {
  name: string;
  value: unknown;
  optional?: boolean;
};
