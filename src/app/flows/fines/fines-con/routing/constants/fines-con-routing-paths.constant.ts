import { IFinesConRoutingPaths } from '../interfaces/fines-con-routing-paths.interface';

export const FINES_CON_ROUTING_PATHS: IFinesConRoutingPaths = {
  root: 'consolidation',
  children: {
    selectBusinessUnit: 'select-business-unit',
  },
};
