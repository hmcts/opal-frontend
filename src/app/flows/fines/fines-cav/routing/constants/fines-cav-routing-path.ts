import { IFinesCavRoutingPaths } from '../interfaces/fines-cav-routing-paths.interface';

export const FINES_CAV_ROUTING_PATHS: IFinesCavRoutingPaths = {
  root: 'check-and-validate',
  children: {
    accounts: 'accounts',
  },
};
