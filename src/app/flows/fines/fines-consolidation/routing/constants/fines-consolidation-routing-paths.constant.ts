import { IFinesConsolidationRoutingPaths } from '../interfaces/fines-consolidation-routing-paths.interface';

export const FINES_CONSOLIDATION_ROUTING_PATHS: IFinesConsolidationRoutingPaths = {
  root: 'consolidation',
  children: {
    selectBusinessUnit: 'select-business-unit',
  },
};
