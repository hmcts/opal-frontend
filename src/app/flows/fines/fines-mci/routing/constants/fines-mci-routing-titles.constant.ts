import { IFinesMciRoutingPaths } from '../interfaces/fines-mci-routing-paths.interface';

export const FINES_MCI_ROUTING_TITLES: IFinesMciRoutingPaths = {
  root: 'Manual cash input',
  children: {
    createAllocate: 'Create and allocate tills',
  },
};
