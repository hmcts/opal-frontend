import { IFinesAccRoutingPaths } from '../interfaces/fines-acc-routing-paths.interface';

export const FINES_ACC_ROUTING_PATHS: IFinesAccRoutingPaths = {
  root: 'account',
  children: {
    details: 'details',
  },
};
