// The common history helper concatenates these prefixes directly with unqualified aliases.
// The empty prefix supports top-level aliases such as `type`; non-empty prefixes need the trailing dot.
export const FINES_ACC_HISTORY_AND_NOTES_DETAILS_ALIAS_PATH_PREFIXES = ['', 'details.', 'data.', 'payload.'];
