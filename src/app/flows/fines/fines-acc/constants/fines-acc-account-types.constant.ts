import { IFinesAccAccountTypes } from '../interfaces/fines-acc-account-types.interface';

export const FINES_ACC_ACCOUNT_TYPES: IFinesAccAccountTypes = {
  fine: 'Fine',
  fixedPenalty: 'Fixed Penalty',
};
