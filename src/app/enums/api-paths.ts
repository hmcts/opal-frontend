const baseUrl = '/api/';
const defendantAccountPath = 'defendant-account/';

export enum ApiPaths {
  defendantAccount = `${baseUrl}${defendantAccountPath}`,
  defendantAccountSearch = `${baseUrl}${defendantAccountPath}`,
}
